# create-rsa-keys



```SHELL
cd $HOME

ls -ltr ~/.ssh

ssh-keygen 

# enter

# enter 

# enter
```

```SHELL
cat ~/.ssh/id_rsa.pub >> ~/.ssh/authorized_keys
```

```SHELL
ssh localhost

# yes

exit
```
